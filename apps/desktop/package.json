{
  "name": "@squirrel-switch/desktop",
  "version": "1.12.0",
  "private": true,
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "pnpm --filter @squirrel-switch/web build && pnpm --filter @squirrel-switch/server build && tsc -p tsconfig.json && electron dist/main.js"
  },
  "devDependencies": {
    "@types/node": "^22.19.1",
    "electron": "^39.2.7",
    "typescript": "^5.9.3"
  }
}
